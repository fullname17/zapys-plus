name: nova
description: "Kavio — универсальная Business OS для бизнеса по записи."
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_riverpod: ^2.5.1   # state management
  go_router: ^14.2.0         # официальная навигация: deep links + web
  drift: ^2.18.0             # local-first БД (SQLite), offline-first
  drift_flutter: ^0.2.0      # кроссплатформенное открытие БД (вкл. web)
  path_provider: ^2.1.3
  url_launcher: ^6.3.0       # звонок/сообщение/WhatsApp из карточки клиента
  intl: ^0.20.2              # форматирование дат/времени/валюты (пиннится flutter_localizations)
  google_fonts: ^6.2.1       # Inter в рантайме (веб) — пиксель-в-пиксель типографика до бандла TTF
  image_picker: ^1.2.3       # фото робіт: камера й галерея (на вебі — вибір файлу)
  web: ^1.1.1                # збереження резервної копії файлом у браузері (Blob + <a download>)

  # --- Опциональные интеграции (адаптеры к портам, см. docs/INTEGRATIONS.md) ---
  # Раскомментируйте нужное и подключите адаптер + override провайдера.
  # firebase_core: ^3.1.0
  # firebase_crashlytics: ^4.0.0      # → CrashReporter
  # firebase_analytics: ^11.0.0       # → AnalyticsService
  # firebase_remote_config: ^5.0.0    # → RemoteConfigService (feature flags)
  # sentry_flutter: ^8.3.0            # → CrashReporter (альтернатива)
  # in_app_purchase: ^3.2.0          # → BillingService (Apple IAP / Play Billing)
  # flutter_stripe: ^11.0.0          # → BillingService (Stripe, web)
  # supabase_flutter: ^2.5.0         # → AuthService / бэкенд-синк

dev_dependencies:
  drift_dev: ^2.18.0         # кодогенерация Drift
  build_runner: ^2.4.11
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  generate: true   # генерация локализаций (i18n) из lib/l10n/*.arb
  # Inter забандлен локально (offline-first): статические начертания лежат в
  # assets/google_fonts/ с именами, которые понимает пакет google_fonts
  # (Inter-Regular.ttf и т.д.). AppTypography продолжает вызывать
  # GoogleFonts.inter(), но шрифт берётся из ассетов, а не из сети — типографика
  # пиксель-в-пиксель и одинакова в CI, офлайн и на устройстве.
  # Веса ровно те, что использует AppTypography: 400/500/600/700/800.
  assets:
    - assets/google_fonts/

# --- Бренд Kavio: генерация иконок и splash ---
# Инструменты подключаются в CI (build.yml) через `dart pub global activate`,
# а НЕ как зависимости проекта — чтобы не влиять на разрешение версий основного
# графа и не рисковать зелёным CI. Исходники знака — assets/brand/*.png
# (сгенерированы из docs/brand/kavio-icon.svg, концепция Confirm).
flutter_launcher_icons:
  image_path: "assets/brand/icon-1024.png"
  android: true
  ios: true
  remove_alpha_ios: true
  min_sdk_android: 21
  adaptive_icon_background: "#5B5BD6"
  adaptive_icon_foreground: "assets/brand/icon-foreground-1024.png"
  web:
    generate: true
    image_path: "assets/brand/icon-1024.png"
    background_color: "#5B5BD6"
    theme_color: "#5B5BD6"

flutter_native_splash:
  color: "#FBFBFC"
  color_dark: "#0B0B0F"
  image: "assets/brand/splash-logo.png"
  image_dark: "assets/brand/splash-logo.png"
  android_12:
    image: "assets/brand/splash-logo.png"
    color: "#FBFBFC"
    color_dark: "#0B0B0F"
  android: true
  ios: true
  web: false
